# HISTORY

ai.sungd.uk (오늘 뭐 써볼까) 변천사. 최신이 맨 위.

## 2026-09-23 — 「모델」 탭: 코딩 모델 비용 효율 표

- **무엇**: 상단 탭에 「모델」을 더했다. Terminal-Bench 4.0(Artificial Analysis, mini-swe-agent)을 모델·effort
  단계마다 잰 43개 설정 가운데 점수가 같거나 높은데 더 싼 설정이 없는 여섯 줄만 구간(최고·균형·저가·초저가)으로 싣는다.
- **왜**: 도구마다 모델·effort 를 고를 때 매번 벤치를 다시 찾아봤다. 도구를 가로지르는 비교라 도구별 칸이 아니라 탭이다.
- **핵심 결정·교훈**: 이 사이트에서 처음으로 손으로 갱신하는 구획이다. 봇이 쓰는 `<div id="data">` 가 아니라 화면 안
  `<!--MODELS-->` 구간에 두고 큐레이션 프롬프트에서 막았다. 새 모델이 나올 때만 갱신하고, 기준일이 90일을 넘기면
  흐려지게 해서 손을 놓아도 낡은 게 보이게 했다. 체크 칸·색 막대는 뺐다 — 숫자 여섯 줄이면 충분하다.

## 2026-09-23 — 세 번째 도구 Antigravity CLI

- **무엇**: 도구 전환에 Antigravity CLI(`agy`)를 더했다. `update_agy.py` 가
  `google-antigravity/antigravity-cli` GitHub Releases 에서 버전블록을 `<!--AG-DATA-->` 구간에 꽂고,
  큐레이션·번역 프롬프트와 워크플로 게이트에 Antigravity 가 들어갔다. 첫 판은 1.0.4~1.2.8(51개 버전)
  자료와 픽 34줄(새로 나온 것·용도별 공통 키)로 시작한다.
- **왜**: Gemini 계열 도구도 쓰기 시작했다. `gemini` CLI 가 아니라 `agy` 를 고른 건 실제로 쓰는 쪽이라서다.
- **핵심 결정·교훈**: 릴리스 노트가 머리글 없는 불릿이라 `Fixed …` 로 시작하면 Fix, 나머지는 기능으로 나눈다.
  거의 매일 릴리스가 나오고 대부분 `Improved …` 라, 큐레이션은 `Added`·`Changed` 만 픽 후보로 본다.
  로고는 공식 SVG 를 못 구해 단색 아치로 그렸다.

## 2026-08-13 — 폰에서 읽히게 고치고, 팝업을 실행 카드로 줄임

- **무엇**: 좁은 화면 배치를 다시 짰다. 용도별이 폰에서 두 칸으로 남아 내용이 820px 로 벌어져
  가로 스크롤이 났고, 새로 나온 것은 설명 칸이 0px 로 찌그러져 제목 86줄이 잘려 있었다.
  한 줄을 두 줄로 나누고 글씨를 전체적으로 한 단 낮췄다. 이어서 자세히 팝업의 네 칸
  (무엇이 바뀌나·좋은 점·해보기·학습)을 **두 칸(해보기·알아둘 것)** 으로 줄이고 134줄을 다시 썼다.
- **왜**: 팝업을 열어도 새로 얻는 게 없다는 느낌이 있었다. 재보니 이유가 있었다 —
  **「좋은 점」은 목록 줄과 134/134 글자까지 같은 문장**이었고, 「무엇이 바뀌나」는 절반(77/134)만
  차 있는 데다 제목과 겹쳤다. 목록 줄이 이미 *무엇*과 *왜*에 답하므로, 줄을 눌렀을 때 남는 질문은
  "그래서 지금 뭘 하면 되는데?" 하나다. 팝업을 읽을거리가 아니라 **실행 카드**로 다시 정의했다.
- **핵심 결정·교훈**:
  - **겹침은 세어 봐야 보인다.** 네 칸이 각자 제 몫을 하는 것처럼 읽혔지만, 목록 줄과 대조해
    유사도를 재니 한 칸은 완전 중복이고 한 칸은 반만 차 있었다. 눈으로는 안 잡혔다.
  - **확인 절차 말고 합격 기준으로 적는다.** "…로드됐는지 확인한다" → "`/plugin list` 에 뜨면 된 것".
    체크 칸을 누를지 바로 판단이 서는 문장이라야 체크리스트가 돈다.
  - **격자로 문단을 짜면 안 된다.** 팝업 본문이 CSS 격자여서 문장 속 `<code>` 가 저마다 칸을
    차지했다 — 글이 토막나고 겹쳤다. 폰만이 아니라 **PC 에서도 내내 깨져 있었는데**, 화면을
    통째로 볼 때만 눈에 띄어서 오래 살아남았다. 내어쓰기로 바꿨다.
  - **명령어 링크를 걷어내고 밖으로 나가는 길을 버전 링크 하나로 모았다.** 명령어마다 걸던
    글자 조각(`#:~:text=…`)은 브라우저가 강조만 하고 그 줄로 안 내려가는 일이 잦았다. 307개를
    글자로 되돌리고, 명령어 자리를 눌러도 자세히가 뜨게 했다. 그 과정에서 예전 날짜 줄 14개에
    버전 링크가 아예 없던 것도 드러나 `data-k` 로 버전을 캐 채웠다 — 그 아래 줄들은 원문에 닿을
    길이 통째로 없었다.
  - **한 겹 줄인 격자는 도로 두 겹이 될 수 있다.** 용도별을 1칸으로 줄여도 머리글 둘이 `grid-row:1`
    로 묶여 있어 보이지 않는 칸이 새로 생겼다. 감추려던 선택자(`:last-of-type`)는 엉뚱한 요소를
    가리키고 있었다. 좁은 화면은 재서 확인해야 안다.

## 2026-08-12 — 뉴스에서 체크리스트로. 두 화면으로 줄이고 이름을 바꿈

- **무엇**: 다섯 화면(홈·Claude Code·Codex·Manus·뉴스)을 **두 탭(새로 나온 것·용도별) × 두 도구(Claude Code·Codex)** 로 줄였다. 이름을 `Agent Edge` → **오늘 뭐 써볼까**, 액센트를 형광 보라(`#6b4cff`) → 종이 결(테라코타 `#c2703a` + 명령어 초록)로 갈았다. 줄마다 체크 칸을 붙이고 상태는 `localStorage` 한 통(`ae-done`)에 담아 **두 화면이 같은 상태를 본다**. 자세히는 커서 옆 팝업으로 띄우고, 픽 129줄에 「해보기」를 신설했다(그전엔 0줄). Codex 픽 36줄은 영어 원문이던 것을 한국어로 옮겼다. 버전블록 321개는 화면에서 빼되 `<div id="data">` 안에 그대로 남겼다.
- **왜**: 「뉴스보다 내가 확인할 기능 관점에서, 실용주의로」. 다섯 화면은 읽을거리 묶음이었지 **할 일 목록**이 아니었다. 매일 아침 여는 이유는 "무슨 일이 있었나"가 아니라 "**오늘 뭘 해볼까**" 여서, 시간 축(새로 나온 것)과 일 축(용도별) 둘만 남기고 나머지는 걷어냈다. 이름도 그 물음을 그대로 썼다.
- **핵심 결정·교훈**:
  - **자료와 화면을 갈랐다.** 버전블록을 지우면 매일 도는 `update_changelog`·`update_codex` 가 꽂을 자리를 잃고 큐레이션이 읽을 원본도 없어진다. 화면에서만 빼고 파일엔 남겼다.
  - **파이프라인이 화면 구조에 묶여 있었다.** `update_codex.py` 가 Codex 구간을 `data-view="codex"`~`data-view="manus"` 로 잡고 있어서 **Manus 화면을 지우는 순간 터진다.** 표식(`<!--CX-DATA-->`)으로 바꿔 화면과 떼어냈다. 화면을 갈아엎을 땐 화면을 읽는 스크립트부터 찾을 것.
  - **두 목록을 잇는 공통 id 가 없었다.** 같은 기능을 픽과 용도별이 서로 다른 이름으로 적어(`/context` ↔ `/context 초과 경고`) 체크 연동이 14개 중 7개만 됐다. 키를 「명령어 첫 낱말+버전」으로 느슨하게 잡아 11개로 늘렸지만 **근본은 큐레이션이 같은 기능에 같은 id 를 찍는 것**이다.
  - 버튼 어휘를 셋으로 갈랐다 — 면 채움(지금 어디), 대괄호(켜고 끄기), 테두리(눌러서 실행). 섞여 있을 땐 무엇이 무엇인지 안 보였다.

## 2026-06-10 — 자동화 전 탭 확대 (Codex·Manus·뉴스) + stage-2 수리

- **무엇**: 그동안 Claude Code 한 탭만 자동 갱신되고 Codex·Manus·뉴스는 ~2-3주 방치였음. ① stage-2 큐레이션이 `claude-code-action@v1`(PR 모드라 워킹트리 미편집)으로 **한 번도 커밋 안 되던 버그를 헤드리스 `claude -p`로 교체**해 수리. ② `update_codex.py` 신규 — openai/codex GitHub Releases(stable만)로 Codex 버전 블록 자동 삽입. ③ `collect_blog.py`에 anthropic.com/news·openai.com 추가. ④ `curate_prompt.md`를 CC·Codex·Manus·뉴스 4탭 커버로 확장.
- **왜**: "Claude Code만 잘 되고 나머지는 죽어있다"는 문제. 큐레이션 파이프라인이 PR 모드 액션이라 워킹트리를 안 고쳐 매번 noop(이력상 큐레이션 커밋 0건)이었고, Codex/Manus/뉴스는 수집 소스 자체가 없었음.
- **핵심 결정·교훈**: Codex는 changelog형이라 **결정적 스크립트**로 버전 블록 생성(LLM 불필요), 써볼·Manus·뉴스 배치만 stage-2 LLM이 "기존 항목 복사→값 교체" 전략으로. 모델 출시 발표(Fable 5)를 컷오프 이후라 "가짜"로 오판했던 사고 → **추측 금지, WebSearch 검증** 룰을 프롬프트에 못박음.

## 2026-05-30 — Agent Edge 리브랜딩 + 5탭 IA 개편

- **무엇**: 단일 Claude Code 중심에서 **5탭 IA(홈·Claude Code·Codex·Manus·뉴스)**로 확장. 이름을 **Agent Edge**로 확정하고 마크를 `>_` 터미널 프롬프트(`>`=전진/edge), 액센트를 보라(`--pick #6b4cff`)로 통일. blog·Manus 항목에 분석 모달(효과성·원리·맥락) + 개념 다이어그램(병렬·양방향·순환·비교) 추가.
- **왜**: Claude Code만으로는 "AI 코딩 에이전트 지형"을 못 담음. Codex·Manus까지 한 화면에서 비교해야 *써볼 가치*가 상대적으로 보인다. 이름 "what to try"/"ai·pick"은 정체성이 약해 브랜드 필요.
- **핵심 결정·교훈**: CC/Codex는 changelog형(버전블록+기간 드롭다운), Manus는 이벤트형(전폭) — **소스 성격에 맞춰 레이아웃을 분기**. 단일 정적 HTML 유지(빌드 없음)로 배포 마찰 0. nav를 상단바에 통합해 모바일 세로 공간 절약.

## 2026-05-29 — MVP 강화: 효과성 ★ 등급 · i18n · 애널리틱스

- **무엇**: 써볼 기능에 **효과성 3단계 골드 ★ 등급 + 정렬** 도입(점 → ★). **한/영 i18n 토글**, GA4·Search Console·meta description, 시니어 프로덕트 디자이너 리디자인(랜드마크 하이라이트, 모노 SVG topbar).
- **왜**: "다 써볼 만하다"는 신호가 아님 — 등급으로 **우선순위**를 강제해야 큐레이션 가치가 생김. 영어 사용자·검색 유입 대비.
- **핵심 결정·교훈**: 효과성 등급은 자동화가 아니라 **판단의 산물** — Claude가 매 릴리스에서 직접 매김. 상대시간은 "N일 전"으로 통일하고 히트맵 색(빨강→흐림)으로 신선도 표현.

## 2026-05-28 — Claude Code changelog 큐레이션 MVP

- **무엇**: 공식 Claude Code changelog(시간순 raw 목록)를 **3가지 시각으로 재구성**하는 단일 정적 HTML. ① 🎯오늘 써볼 기능 6개(왜 효과적+학습포인트) ② 🧰용도별 8 카테고리 ③ 버전별 changelog 블록(Feature 한 줄·개선/보안/수정 탭 접기). 명령어→공식 docs deep-link(Text Fragment), 며칠전 히트맵.
- **왜**: 매주 1~2개씩 쏟아지는 릴리스에서 *정말 써볼 만한 것*을 raw 목록으로는 가려내기 어렵다. 같은 데이터를 **신호 중심으로 재배치**하면 노이즈가 걸러진다.
- **핵심 결정·교훈**: **매일 05:20 KST GitHub Actions 자동화** — `update_changelog.py`(토큰 0)가 버전 감지·분류·삽입하고, 새 feature 있는 날만 Claude Code Action이 🎯Pick·🧰용도별을 갱신. **구독 토큰(API 키 X)**으로 per-token 과금 0. 대부분의 날은 무료 1단계만 돈다.
- **본질**: 노이즈에서 신호 거르기 + 가치 판단 + 학습/성장. → GitHub Pages → ai.sungd.uk.
